interface:
  display_name: "Portfolio Site"
  short_description: "Repo-specific patterns and workflows for portfolio-site"
  default_prompt: "Use the portfolio-site repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true